import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette:{
        primary:{
            main: "#1760a5",
            light: "blue"
        },
        secondary: {
            main: "#15c630"
        },
        otherColor:{
            main: "grey"
        }
    }
})